import { TypedeciderPipe } from './type-decider.pipe';

describe('TypedeciderPipe', () => {
  it('create an instance', () => {
    const pipe = new TypedeciderPipe();
    expect(pipe).toBeTruthy();
  });
});
